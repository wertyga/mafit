query calculatePFC($data: PFCInput) {
	calculatePFC(data: $data) {
		calculateFormula {
			fats
			protein
			carbs
		}
		dailyProtein
		dailyFats
		dailyCarbs
		minCal
		normaCal
		recommendCal
	}
}
